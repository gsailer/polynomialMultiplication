-- Polynomial multiplication
poly1=[0,6,2,3,1]
poly2=[0,0,1]
-- desired result: [0,0,0,6,2,3,1,0]
-- multiply each element in poly1 with each el in poly2 and put in in pos [pos(poly1[el])+pos(poly2[el]))]
result=
